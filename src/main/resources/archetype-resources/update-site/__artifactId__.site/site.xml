#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<?xml version="1.0" encoding="UTF-8"?>
<site>
   <description name="${pluginName} Update Site" url="${updateSiteLocation}">
      ${pluginName} Update Site
   </description>
   <feature url="features/${artifactId}.feature_${version}.qualifier.jar" id="${artifactId}.feature" version="${version}.qualifier">
      <category name="${artifactId}_category"/>
   </feature>
   <category-def name="${artifactId}_category" label="${pluginCategory}">
      <description>
         ${pluginCategory}
      </description>
   </category-def>
</site>
